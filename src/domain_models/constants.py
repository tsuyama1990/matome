"""
Configuration constants for the Matome system.
Primarily used for environment variable keys or centralized constant values
that are not default configuration values.
"""

# Default values are now in ProcessingConfig.
# We keep these only if needed by other non-config modules, but the auditor request was strict.
# "NO hardcoded settings. All config via config.py/Pydantic"
# I will clear this file effectively as requested in plan step 3.

__all__: list[str] = []
